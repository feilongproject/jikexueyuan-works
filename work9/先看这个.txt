感觉这个比上一任务使用php做更难

感觉智商不够用

而课程里可参考的地方实在太少，基本都是找别人的模块照着做

基本由任务8修改过来，但是应该比任务8的bug多

这里有一个比较严重的bug是，后台插入列表时，遇到重复标题时提交会导致404错误，
并不是因为标题重复而使该sql语句未执行，在php可以判定语句是否执行了，而这里执行成功也会成功输出result结果
（controlers/news.con.js37-40行），失败没有输出（按道理result.affectedRows为0）直接404
另外，这里有时即使成功插入也会404报错（能成功操作数据库，提交后404，返回重新刷新会发现成功插入数据）
而有时也可以直接插入，并没有任何错误......


另外希望老师今次能尽量提出这次作业出现的问题及修改建议和思路，nodejs还是不太会用，智商不够ORZ.......

用户admin密码admin


node app.js
localhost:8000
右上点击去后台

前端读取应该基本没有什么问题，主要是后台操作......

